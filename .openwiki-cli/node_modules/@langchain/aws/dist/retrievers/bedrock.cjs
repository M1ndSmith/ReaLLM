let _aws_sdk_client_bedrock_agent_runtime = require("@aws-sdk/client-bedrock-agent-runtime");
let _langchain_core_retrievers = require("@langchain/core/retrievers");
//#region src/retrievers/bedrock.ts
/**
* Class for interacting with Amazon Bedrock Knowledge Bases, a RAG workflow oriented service
* provided by AWS. Extends the BaseRetriever class.
* @example
* ```typescript
* const retriever = new AmazonKnowledgeBaseRetriever({
*   topK: 10,
*   knowledgeBaseId: "YOUR_KNOWLEDGE_BASE_ID",
*   region: "us-east-2",
*   clientOptions: {
*     credentials: {
*       accessKeyId: "YOUR_ACCESS_KEY_ID",
*       secretAccessKey: "YOUR_SECRET_ACCESS_KEY",
*     },
*   },
* });
*
* const docs = await retriever.getRelevantDocuments("How are clouds formed?");
* ```
*/
var AmazonKnowledgeBaseRetriever = class extends _langchain_core_retrievers.BaseRetriever {
	static lc_name() {
		return "AmazonKnowledgeBaseRetriever";
	}
	lc_namespace = [
		"langchain",
		"retrievers",
		"amazon_bedrock_knowledge_base"
	];
	knowledgeBaseId;
	topK;
	bedrockAgentRuntimeClient;
	filter;
	overrideSearchType;
	constructor({ knowledgeBaseId, topK = 10, clientOptions, region, filter, overrideSearchType }) {
		super();
		this.topK = topK;
		this.filter = filter;
		this.overrideSearchType = overrideSearchType;
		this.bedrockAgentRuntimeClient = new _aws_sdk_client_bedrock_agent_runtime.BedrockAgentRuntimeClient({
			region,
			...clientOptions
		});
		this.knowledgeBaseId = knowledgeBaseId;
	}
	/**
	* Cleans the result text by replacing sequences of whitespace with a
	* single space and removing ellipses.
	* @param resText The result text to clean.
	* @returns The cleaned result text.
	*/
	cleanResult(resText) {
		return resText.replace(/\s+/g, " ").replace(/\.\.\./g, "");
	}
	async queryKnowledgeBase(query, topK, filter, overrideSearchType) {
		const retrieveCommand = new _aws_sdk_client_bedrock_agent_runtime.RetrieveCommand({
			knowledgeBaseId: this.knowledgeBaseId,
			retrievalQuery: { text: query },
			retrievalConfiguration: { vectorSearchConfiguration: {
				numberOfResults: topK,
				overrideSearchType,
				filter
			} }
		});
		return (await this.bedrockAgentRuntimeClient.send(retrieveCommand)).retrievalResults?.map((result) => {
			let source;
			switch (result.location?.type) {
				case "CONFLUENCE":
					source = result.location?.confluenceLocation?.url;
					break;
				case "S3":
					source = result.location?.s3Location?.uri;
					break;
				case "SALESFORCE":
					source = result.location?.salesforceLocation?.url;
					break;
				case "SHAREPOINT":
					source = result.location?.sharePointLocation?.url;
					break;
				case "WEB":
					source = result.location?.webLocation?.url;
					break;
				default: source = result.location?.s3Location?.uri;
			}
			return {
				pageContent: this.cleanResult(result.content?.text || ""),
				metadata: {
					source,
					score: result.score,
					...result.metadata
				}
			};
		}) ?? [];
	}
	async _getRelevantDocuments(query) {
		return await this.queryKnowledgeBase(query, this.topK, this.filter, this.overrideSearchType);
	}
};
//#endregion
exports.AmazonKnowledgeBaseRetriever = AmazonKnowledgeBaseRetriever;

//# sourceMappingURL=bedrock.cjs.map