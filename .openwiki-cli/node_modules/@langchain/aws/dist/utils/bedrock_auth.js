import { getEnvironmentVariable } from "@langchain/core/utils/env";
//#region src/utils/bedrock_auth.ts
const AWS_BEARER_TOKEN_BEDROCK = "AWS_BEARER_TOKEN_BEDROCK";
function resolveBedrockBearerToken(token) {
	return token ?? getEnvironmentVariable("AWS_BEARER_TOKEN_BEDROCK");
}
function createBedrockBearerTokenClientConfig(token) {
	if (!token) return {};
	return {
		authSchemePreference: ["httpBearerAuth"],
		token: async () => ({ token })
	};
}
//#endregion
export { AWS_BEARER_TOKEN_BEDROCK, createBedrockBearerTokenClientConfig, resolveBedrockBearerToken };

//# sourceMappingURL=bedrock_auth.js.map