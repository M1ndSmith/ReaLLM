//#region src/utils/errors.ts
function extractBedrockErrorMessage(error) {
	if (typeof error === "string") return error;
	if (!error || typeof error !== "object") return;
	if ("message" in error && typeof error.message === "string") return error.message;
	if ("Message" in error && typeof error.Message === "string") return error.Message;
	if ("errors" in error && Array.isArray(error.errors)) {
		const messages = error.errors.map((item) => {
			if (typeof item === "string") return item;
			if (item && typeof item === "object" && "message" in item && typeof item.message === "string") return item.message;
		}).filter((message) => typeof message === "string");
		if (messages.length > 0) return messages.join("; ");
	}
}
function normalizeBedrockError(error) {
	if (error instanceof Error) return error;
	const message = extractBedrockErrorMessage(error) ?? "An error occurred while calling Bedrock Converse.";
	return new Error(message, { cause: error });
}
//#endregion
export { normalizeBedrockError };

//# sourceMappingURL=errors.js.map