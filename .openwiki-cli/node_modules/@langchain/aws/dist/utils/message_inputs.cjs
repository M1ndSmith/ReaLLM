const require_compat = require("./compat.cjs");
let _langchain_core_messages = require("@langchain/core/messages");
//#region src/utils/message_inputs.ts
function isDefaultCachePoint(block) {
	if (!(typeof block === "object" && block !== null && "cachePoint" in block && block.cachePoint && typeof block.cachePoint === "object" && block.cachePoint !== null && "type" in block.cachePoint && block.cachePoint.type === "default")) return false;
	const { ttl } = block.cachePoint;
	return ttl === void 0 || ttl === "5m" || ttl === "1h";
}
function convertCachePointBlock(block) {
	const { ttl } = block.cachePoint;
	return { cachePoint: {
		type: "default",
		...ttl !== void 0 ? { ttl } : {}
	} };
}
function isConverseCachePoint(block) {
	return Boolean(typeof block === "object" && block !== null && "cachePoint" in block && block.cachePoint && typeof block.cachePoint === "object" && block.cachePoint !== null && "type" in block.cachePoint);
}
function createConverseCachePointBlock(cacheControl, isNovaModel) {
	const ttl = !isNovaModel && cacheControl.ttl && cacheControl.ttl !== "5m" ? cacheControl.ttl : void 0;
	return { cachePoint: {
		type: "default",
		...ttl ? { ttl } : {}
	} };
}
function applyCachePointsToConversePayload(fields) {
	const { cacheControl, system, messages, params, modelId } = fields;
	if (!cacheControl) return;
	const isNovaModel = modelId.toLowerCase().includes("amazon.nova");
	const cacheBlock = createConverseCachePointBlock(cacheControl, isNovaModel);
	if (system.length > 0 && !system.some((block) => isConverseCachePoint(block))) system.push(cacheBlock);
	const lastContent = messages[messages.length - 1]?.content;
	if (Array.isArray(lastContent)) {
		if (!(isNovaModel && lastContent.some((block) => typeof block === "object" && block !== null && ("toolResult" in block || "toolUse" in block))) && !lastContent.some((block) => isConverseCachePoint(block))) lastContent.push(cacheBlock);
	}
	const tools = params?.toolConfig?.tools;
	if (!isNovaModel && Array.isArray(tools) && !tools.some((tool) => isConverseCachePoint(tool))) tools.push(cacheBlock);
}
function extractImageInfo(base64) {
	const formatMatch = base64.match(/^data:image\/(\w+);base64,/);
	let format;
	if (formatMatch) {
		const extractedFormat = formatMatch[1].toLowerCase();
		if ([
			"gif",
			"jpeg",
			"png",
			"webp"
		].includes(extractedFormat)) format = extractedFormat;
	}
	const base64Data = base64.replace(/^data:image\/\w+;base64,/, "");
	const binaryString = atob(base64Data);
	const bytes = new Uint8Array(binaryString.length);
	for (let i = 0; i < binaryString.length; i += 1) bytes[i] = binaryString.charCodeAt(i);
	return { image: {
		format,
		source: { bytes }
	} };
}
const mimeTypeToVideoFormat = {
	"video/flv": "flv",
	"video/mkv": "mkv",
	"video/mov": "mov",
	"video/mp4": "mp4",
	"video/mpeg": "mpeg",
	"video/mpg": "mpg",
	"video/three_gp": "three_gp",
	"video/webm": "webm",
	"video/wmv": "wmv"
};
const mimeTypeToAudioFormat = {
	"audio/aac": "aac",
	"audio/flac": "flac",
	"audio/m4a": "m4a",
	"audio/mka": "mka",
	"audio/mkv": "mkv",
	"audio/mp3": "mp3",
	"audio/mp4": "mp4",
	"audio/mpeg": "mpeg",
	"audio/mpga": "mpga",
	"audio/ogg": "ogg",
	"audio/opus": "opus",
	"audio/pcm": "pcm",
	"audio/wav": "wav",
	"audio/webm": "webm",
	"audio/x-aac": "x-aac"
};
function resolveMediaSource(block) {
	if (typeof block.data === "string") return { bytes: Uint8Array.from(atob(block.data), (c) => c.charCodeAt(0)) };
	if (block.data instanceof Uint8Array) return { bytes: block.data };
	if (typeof block.url === "string") {
		const parsedData = (0, _langchain_core_messages.parseBase64DataUrl)({
			dataUrl: block.url,
			asTypedArray: true
		});
		if (parsedData) return { bytes: parsedData.data };
		throw new Error(`Only base64 data URLs are supported for ${block.type} blocks with 'url' field with ChatBedrockConverse.`);
	}
	if (typeof block.fileId === "string") return { s3Location: { uri: block.fileId } };
	throw new Error(`${block.type} block must include one of: 'data' (base64 string or Uint8Array), 'url' (base64 data URL), or 'fileId' (S3 URI).`);
}
function convertMultimodalVideoBlock(block) {
	const mimeType = block.mimeType;
	let format;
	if (mimeType) {
		format = mimeTypeToVideoFormat[mimeType];
		if (!format) format = (0, _langchain_core_messages.parseMimeType)(mimeType).subtype;
	}
	const source = resolveMediaSource(block);
	return { video: {
		format,
		source
	} };
}
function convertMultimodalAudioBlock(block) {
	const mimeType = block.mimeType;
	let format;
	if (mimeType) {
		format = mimeTypeToAudioFormat[mimeType];
		if (!format) format = (0, _langchain_core_messages.parseMimeType)(mimeType).subtype;
	}
	const source = resolveMediaSource(block);
	return { audio: {
		format,
		source
	} };
}
const standardContentBlockConverter = {
	providerName: "ChatBedrockConverse",
	fromStandardTextBlock(block) {
		return { text: block.text };
	},
	fromStandardImageBlock(block) {
		let format;
		if (block.source_type === "url") {
			const parsedData = (0, _langchain_core_messages.parseBase64DataUrl)({
				dataUrl: block.url,
				asTypedArray: true
			});
			if (parsedData) {
				format = (0, _langchain_core_messages.parseMimeType)(parsedData.mime_type).type;
				return { image: {
					format,
					source: { bytes: parsedData.data }
				} };
			} else throw new Error("Only base64 data URLs are supported for image blocks with source type 'url' with ChatBedrockConverse.");
		} else if (block.source_type === "base64") {
			if (block.mime_type) format = (0, _langchain_core_messages.parseMimeType)(block.mime_type).subtype;
			if (format && ![
				"gif",
				"jpeg",
				"png",
				"webp"
			].includes(format)) throw new Error(`Unsupported image mime type: "${block.mime_type}" ChatBedrockConverse only supports "image/gif", "image/jpeg", "image/png", and "image/webp" formats.`);
			return { image: {
				format,
				source: { bytes: Uint8Array.from(atob(block.data), (c) => c.charCodeAt(0)) }
			} };
		} else if (block.source_type === "id") throw new Error("Image source type 'id' not supported with ChatBedrockConverse.");
		else throw new Error(`Unsupported image source type: "${block.source_type}" with ChatBedrockConverse.`);
	},
	fromStandardFileBlock(block) {
		const mimeTypeToDocumentFormat = {
			"text/csv": "csv",
			"application/msword": "doc",
			"application/vnd.openxmlformats-officedocument.wordprocessingml.document": "docx",
			"text/html": "html",
			"text/markdown": "md",
			"application/pdf": "pdf",
			"text/plain": "txt",
			"application/vnd.ms-excel": "xls",
			"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": "xlsx"
		};
		const name = block.metadata?.name ?? block.metadata?.filename ?? block.metadata?.title ?? crypto.randomUUID().replace(/-/g, "").slice(0, 12);
		if (block.source_type === "text") return { document: {
			name,
			format: "txt",
			source: { bytes: new TextEncoder().encode(block.text) }
		} };
		if (block.source_type === "url") {
			const parsedData = (0, _langchain_core_messages.parseBase64DataUrl)({
				dataUrl: block.url,
				asTypedArray: true
			});
			if (parsedData) {
				const parsedMimeType = (0, _langchain_core_messages.parseMimeType)(parsedData.mime_type ?? block.mime_type);
				return { document: {
					name,
					format: mimeTypeToDocumentFormat[`${parsedMimeType.type}/${parsedMimeType.subtype}`],
					source: { bytes: parsedData.data }
				} };
			}
			throw new Error("Only base64 data URLs are supported for file blocks with source type 'url' with ChatBedrockConverse.");
		}
		if (block.source_type === "base64") {
			let format;
			if (block.mime_type) {
				const parsedMimeType = (0, _langchain_core_messages.parseMimeType)(block.mime_type);
				format = mimeTypeToDocumentFormat[`${parsedMimeType.type}/${parsedMimeType.subtype}`];
				if (format === void 0) throw new Error(`Unsupported file mime type: "${block.mime_type}" ChatBedrockConverse only supports ${Object.keys(mimeTypeToDocumentFormat).join(", ")} formats.`);
			}
			return { document: {
				name,
				format,
				source: { bytes: Uint8Array.from(atob(block.data), (c) => c.charCodeAt(0)) }
			} };
		}
		if (block.source_type === "id") throw new Error("File source type 'id' not supported with ChatBedrockConverse.");
		throw new Error(`Unsupported file source type: "${block.source_type}" with ChatBedrockConverse.`);
	}
};
function convertLangChainContentBlockToConverseContentBlock({ block, onUnknown = "throw" }) {
	if (typeof block === "string") return { text: block };
	if ((0, _langchain_core_messages.isDataContentBlock)(block)) return (0, _langchain_core_messages.convertToProviderContentBlock)(block, standardContentBlockConverter);
	if (block.type === "text") return { text: block.text };
	if (block.type === "image_url") return extractImageInfo(typeof block.image_url === "string" ? block.image_url : block.image_url.url);
	if (block.type === "document" && block.document !== void 0) return { document: block.document };
	if (block.type === "image" && block.image !== void 0) return { image: block.image };
	if (block.type === "video" && block.video !== void 0) return { video: block.video };
	if (block.type === "video") return convertMultimodalVideoBlock(block);
	if (block.type === "audio" && block.audio !== void 0) return { audio: block.audio };
	if (block.type === "audio") return convertMultimodalAudioBlock(block);
	if (isDefaultCachePoint(block)) return convertCachePointBlock(block);
	if (onUnknown === "throw") throw new Error(`Unsupported content block type: ${block.type}`);
	else return block;
}
function convertSystemMessageToConverseMessage(msg) {
	if (typeof msg.content === "string") return [{ text: msg.content }];
	else if (Array.isArray(msg.content) && msg.content.length > 0) {
		const contentBlocks = [];
		for (const block of msg.content) if (block.type === "text" && typeof block.text === "string") contentBlocks.push({ text: block.text });
		else if (isDefaultCachePoint(block)) contentBlocks.push(convertCachePointBlock(block));
		else break;
		if (msg.content.length === contentBlocks.length) return contentBlocks;
	}
	throw new Error("System message content must be either a string, or an array of text blocks, optionally including a cache point.");
}
function convertAIMessageToConverseMessage(msg) {
	if (msg.response_metadata?.output_version === "v1") return {
		role: "assistant",
		content: require_compat.convertFromV1ToChatBedrockConverseMessage(msg)
	};
	const assistantMsg = {
		role: "assistant",
		content: []
	};
	if (typeof msg.content === "string" && msg.content !== "") assistantMsg.content?.push({ text: msg.content });
	else if (Array.isArray(msg.content)) {
		const concatenatedBlocks = concatenateLangchainReasoningBlocks(msg.content);
		const contentBlocks = [];
		concatenatedBlocks.forEach((block) => {
			if (block.type === "text" && block.text !== "") {
				if (block.text?.replace(/\n/g, "").trim() === "") {
					if (contentBlocks.length > 0) {
						const mergedTextContent = `${contentBlocks[contentBlocks.length - 1].text}${block.text}`;
						contentBlocks[contentBlocks.length - 1].text = mergedTextContent;
					}
				} else contentBlocks.push({ text: block.text });
			} else if (block.type === "reasoning" || block.type === "reasoning_content") {
				const reasoningContent = langchainReasoningBlockToBedrockReasoningBlock(block);
				if (reasoningContent) contentBlocks.push({ reasoningContent });
			} else if (isDefaultCachePoint(block)) contentBlocks.push(convertCachePointBlock(block));
			else {
				const blockValues = Object.fromEntries(Object.entries(block).filter(([key]) => key !== "type"));
				throw new Error(`Unsupported content block type: ${block.type} with content of ${JSON.stringify(blockValues, null, 2)}`);
			}
		});
		assistantMsg.content = [...assistantMsg.content ? assistantMsg.content : [], ...contentBlocks];
	}
	if (msg.tool_calls && msg.tool_calls.length) assistantMsg.content = [...assistantMsg.content ? assistantMsg.content : [], ...msg.tool_calls.map((tc) => ({ toolUse: {
		toolUseId: tc.id,
		name: tc.name,
		input: tc.args
	} }))];
	return assistantMsg;
}
function convertHumanMessageToConverseMessage(msg) {
	if (msg.content === "") throw new Error(`Invalid message content: empty string. '${msg.type}' must contain non-empty content.`);
	return {
		role: "user",
		content: (Array.isArray(msg.content) ? msg.content : [msg.content]).map((c) => convertLangChainContentBlockToConverseContentBlock({
			block: c,
			onUnknown: "throw"
		}))
	};
}
function convertToolMessageToConverseMessage(msg) {
	const castMsg = msg;
	if (typeof castMsg.content === "string") return {
		role: "user",
		content: [{ toolResult: {
			toolUseId: castMsg.tool_call_id,
			content: [{ text: castMsg.content }]
		} }]
	};
	else {
		const toolResultContentBlocks = [];
		const cacheContentBlocks = [];
		for (const c of msg.content) {
			const converted = convertLangChainContentBlockToConverseContentBlock({
				block: c,
				onUnknown: "returnUnmodified"
			});
			if (isDefaultCachePoint(c)) cacheContentBlocks.push(converted);
			else if (converted !== c) toolResultContentBlocks.push(converted);
			else toolResultContentBlocks.push({ json: c });
		}
		return {
			role: "user",
			content: [{ toolResult: {
				toolUseId: castMsg.tool_call_id,
				content: toolResultContentBlocks
			} }, ...cacheContentBlocks]
		};
	}
}
function convertToConverseMessages(messages) {
	const converseSystem = messages.reduce((acc, msg) => {
		if (_langchain_core_messages.SystemMessage.isInstance(msg)) acc.push(...convertSystemMessageToConverseMessage(msg));
		return acc;
	}, []);
	return {
		converseMessages: messages.filter((msg) => !_langchain_core_messages.SystemMessage.isInstance(msg)).map((msg) => {
			if (_langchain_core_messages.AIMessage.isInstance(msg)) return convertAIMessageToConverseMessage(msg);
			else if (_langchain_core_messages.HumanMessage.isInstance(msg) || _langchain_core_messages.ChatMessage.isInstance(msg)) return convertHumanMessageToConverseMessage(msg);
			else if (_langchain_core_messages.ToolMessage.isInstance(msg)) return convertToolMessageToConverseMessage(msg);
			else throw new Error(`Unsupported message type: ${msg.type}`);
		}).reduce((acc, curr) => {
			const lastMessage = acc[acc.length - 1];
			if (lastMessage && lastMessage.role === "user" && lastMessage.content?.some((c) => "toolResult" in c) && curr.role === "user" && curr.content?.some((c) => "toolResult" in c)) lastMessage.content = lastMessage.content.concat(curr.content);
			else acc.push(curr);
			return acc;
		}, []),
		converseSystem
	};
}
function langchainReasoningBlockToBedrockReasoningBlock(content) {
	if (typeof content !== "object" || content === null || !("type" in content)) return;
	if (content.type === "reasoning" && "reasoning" in content && typeof content.reasoning === "string" && content.reasoning.length > 0) {
		const signature = "signature" in content && typeof content.signature === "string" ? content.signature : void 0;
		return { reasoningText: {
			text: content.reasoning,
			...signature !== void 0 ? { signature } : {}
		} };
	}
	if (content.type !== "reasoning_content") return;
	if ("reasoningText" in content && typeof content.reasoningText === "object" && content.reasoningText !== null && "text" in content.reasoningText && typeof content.reasoningText.text === "string" && content.reasoningText.text.length > 0) {
		const signature = "signature" in content.reasoningText && typeof content.reasoningText.signature === "string" ? content.reasoningText.signature : void 0;
		return { reasoningText: {
			text: content.reasoningText.text,
			...signature !== void 0 ? { signature } : {}
		} };
	}
	if ("redactedContent" in content && typeof content.redactedContent === "string" && content.redactedContent.length > 0) return { redactedContent: Buffer.from(content.redactedContent, "base64") };
}
function concatenateLangchainReasoningBlocks(content) {
	const concatenatedBlocks = [];
	let concatenatedBlock = {};
	for (const block of content) {
		if (block.type !== "reasoning_content") {
			if (Object.keys(concatenatedBlock).length > 0) {
				concatenatedBlocks.push(concatenatedBlock);
				concatenatedBlock = {};
			}
			concatenatedBlocks.push(block);
			continue;
		}
		if ("reasoningText" in block && typeof block.reasoningText === "object") {
			if ("redactedContent" in concatenatedBlock) {
				concatenatedBlocks.push(concatenatedBlock);
				concatenatedBlock = {};
			}
			const { text, signature } = block.reasoningText;
			const { text: prevText, signature: prevSignature } = "reasoningText" in concatenatedBlock ? concatenatedBlock.reasoningText : {};
			concatenatedBlock = {
				type: "reasoning_content",
				reasoningText: {
					...concatenatedBlock.reasoningText ?? {},
					...prevText !== void 0 || text !== void 0 ? { text: (prevText ?? "") + (text ?? "") } : {},
					...prevSignature !== void 0 || signature !== void 0 ? { signature: (prevSignature ?? "") + (signature ?? "") } : {}
				}
			};
			if ("signature" in block.reasoningText) {
				concatenatedBlocks.push(concatenatedBlock);
				concatenatedBlock = {};
			}
		}
		if ("redactedContent" in block) {
			if ("reasoningText" in concatenatedBlock) {
				concatenatedBlocks.push(concatenatedBlock);
				concatenatedBlock = {};
			}
			const { redactedContent } = block;
			const prevRedactedContent = "redactedContent" in concatenatedBlock ? concatenatedBlock.redactedContent : void 0;
			concatenatedBlock = {
				type: "reasoning_content",
				redactedContent: (prevRedactedContent ? Buffer.concat([Buffer.from(prevRedactedContent, "base64"), Buffer.from(redactedContent, "base64")]) : Buffer.from(redactedContent, "base64")).toString("base64")
			};
		}
	}
	if (Object.keys(concatenatedBlock).length > 0) concatenatedBlocks.push(concatenatedBlock);
	return concatenatedBlocks;
}
//#endregion
exports.applyCachePointsToConversePayload = applyCachePointsToConversePayload;
exports.concatenateLangchainReasoningBlocks = concatenateLangchainReasoningBlocks;
exports.convertToConverseMessages = convertToConverseMessages;
exports.extractImageInfo = extractImageInfo;
exports.langchainReasoningBlockToBedrockReasoningBlock = langchainReasoningBlockToBedrockReasoningBlock;

//# sourceMappingURL=message_inputs.cjs.map