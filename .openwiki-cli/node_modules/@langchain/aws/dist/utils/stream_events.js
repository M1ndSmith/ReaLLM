import { finalizeContentBlock } from "@langchain/core/language_models/compat";
//#region src/utils/stream_events.ts
async function* convertBedrockConverseStream(source, options = {}) {
	const shouldStreamUsage = options.streamUsage ?? true;
	const blockAccumulators = /* @__PURE__ */ new Map();
	let messageStarted = false;
	let usageSnapshot;
	let finishReason;
	for await (const event of source) {
		if (!messageStarted) {
			messageStarted = true;
			yield { event: "message-start" };
		}
		if (event.contentBlockStart) {
			const index = event.contentBlockStart.contentBlockIndex ?? 0;
			const toolUse = event.contentBlockStart.start?.toolUse;
			if (toolUse) {
				const initial = {
					type: "tool_call_chunk",
					id: toolUse.toolUseId,
					name: toolUse.name,
					args: "",
					index
				};
				blockAccumulators.set(index, { ...initial });
				yield {
					event: "content-block-start",
					index,
					content: initial
				};
			}
		} else if (event.contentBlockDelta) {
			const index = event.contentBlockDelta.contentBlockIndex ?? 0;
			const delta = event.contentBlockDelta.delta;
			if (!delta) continue;
			if (typeof delta.text === "string") {
				if (!blockAccumulators.has(index)) {
					const initial = {
						type: "text",
						text: ""
					};
					blockAccumulators.set(index, { ...initial });
					yield {
						event: "content-block-start",
						index,
						content: initial
					};
				}
				const acc = blockAccumulators.get(index);
				acc.text = (acc.text ?? "") + delta.text;
				yield {
					event: "content-block-delta",
					index,
					delta: {
						type: "text-delta",
						text: delta.text
					}
				};
			} else if (delta.toolUse?.input) {
				const acc = blockAccumulators.get(index);
				if (!acc) continue;
				acc.args = (acc.args ?? "") + delta.toolUse.input;
				yield {
					event: "content-block-delta",
					index,
					delta: {
						type: "block-delta",
						fields: {
							type: "tool_call_chunk",
							...acc.id != null ? { id: acc.id } : {},
							...acc.name != null ? { name: acc.name } : {},
							args: acc.args
						}
					}
				};
			} else if (delta.reasoningContent) {
				const text = delta.reasoningContent.text;
				if (typeof text === "string") {
					if (!blockAccumulators.has(index)) {
						const initial = {
							type: "reasoning",
							reasoning: ""
						};
						blockAccumulators.set(index, { ...initial });
						yield {
							event: "content-block-start",
							index,
							content: initial
						};
					}
					const acc = blockAccumulators.get(index);
					acc.reasoning = (acc.reasoning ?? "") + text;
					yield {
						event: "content-block-delta",
						index,
						delta: {
							type: "reasoning-delta",
							reasoning: text
						}
					};
				}
			}
		} else if (event.metadata?.usage && shouldStreamUsage) {
			const u = event.metadata.usage;
			const cacheRead = u.cacheReadInputTokens ?? 0;
			const cacheWrite = u.cacheWriteInputTokens ?? 0;
			const input = (u.inputTokens ?? 0) + cacheRead + cacheWrite;
			const output = u.outputTokens ?? 0;
			usageSnapshot = {
				input_tokens: input,
				output_tokens: output,
				total_tokens: u.totalTokens ?? input + output,
				input_token_details: {
					...u.cacheReadInputTokens != null ? { cache_read: u.cacheReadInputTokens } : {},
					...u.cacheWriteInputTokens != null ? { cache_creation: u.cacheWriteInputTokens } : {}
				}
			};
			yield {
				event: "usage",
				usage: usageSnapshot
			};
		} else if (event.messageStop?.stopReason) finishReason = mapStopReason(event.messageStop.stopReason);
		else yield {
			event: "provider",
			provider: "bedrock-converse",
			name: "stream_event",
			payload: event
		};
	}
	for (const [index, acc] of blockAccumulators) yield {
		event: "content-block-finish",
		index,
		content: finalizeContentBlock(acc)
	};
	yield {
		event: "message-finish",
		reason: finishReason,
		...usageSnapshot ? { usage: usageSnapshot } : {},
		responseMetadata: { model_provider: "bedrock-converse" }
	};
}
function mapStopReason(stopReason) {
	switch (stopReason) {
		case "end_turn":
		case "stop_sequence": return "stop";
		case "tool_use": return "tool_use";
		case "max_tokens": return "length";
		case "guardrail_intervened":
		case "content_filtered": return "content_filter";
		default: return "stop";
	}
}
//#endregion
export { convertBedrockConverseStream };

//# sourceMappingURL=stream_events.js.map