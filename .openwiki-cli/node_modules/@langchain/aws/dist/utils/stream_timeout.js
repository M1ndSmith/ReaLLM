//#region src/utils/stream_timeout.ts
/** Default milliseconds to wait for the first or next Bedrock stream chunk. */
const DEFAULT_STREAM_IDLE_TIMEOUT = 6e4;
/** Resolves stream idle timeout milliseconds; `0` disables the watchdog. */
function resolveStreamIdleTimeout(timeout) {
	if (timeout === void 0 || timeout === 0) return;
	if (!Number.isFinite(timeout) || timeout < 0) throw new Error("streamIdleTimeout must be a non-negative finite number of milliseconds.");
	return timeout;
}
/** Creates the catchable error thrown when Bedrock stops responding within the idle timeout. */
function createStreamIdleTimeoutError(timeout, phase = "next chunk") {
	const error = /* @__PURE__ */ new Error(`Bedrock Converse timed out after ${timeout} ms while waiting for the ${phase}.`);
	error.lc_error_code = "MODEL_STREAM_TIMEOUT";
	return error;
}
/** Creates an AbortController that follows an optional caller-provided signal. */
function createLinkedAbortController(signal) {
	const abortController = new AbortController();
	if (!signal) return {
		abortController,
		cleanup: () => void 0
	};
	if (signal.aborted) {
		abortController.abort(signal.reason);
		return {
			abortController,
			cleanup: () => void 0
		};
	}
	const onAbort = () => abortController.abort(signal.reason);
	signal.addEventListener("abort", onAbort, { once: true });
	return {
		abortController,
		cleanup: () => signal.removeEventListener("abort", onAbort)
	};
}
/** Races a pending request against the idle timeout, aborting on a pre-response stall. */
async function withRequestIdleTimeout(request, timeout, abortController) {
	if (timeout === void 0) return request;
	let timeoutId;
	try {
		return await Promise.race([request, new Promise((_, reject) => {
			timeoutId = setTimeout(() => {
				const error = createStreamIdleTimeoutError(timeout, "initial response");
				abortController.abort(error);
				reject(error);
			}, timeout);
		})]);
	} finally {
		if (timeoutId !== void 0) clearTimeout(timeoutId);
	}
}
/** Wraps a stream with a first/inter-chunk idle timeout that aborts on stalls. */
async function* withStreamIdleTimeout(source, timeout, abortController) {
	if (timeout === void 0) {
		yield* source;
		return;
	}
	const iterator = source[Symbol.asyncIterator]();
	let completed = false;
	let timeoutId;
	try {
		while (true) {
			const nextChunk = iterator.next();
			nextChunk.catch(() => void 0);
			try {
				const result = await Promise.race([nextChunk, new Promise((_, reject) => {
					timeoutId = setTimeout(() => {
						const error = createStreamIdleTimeoutError(timeout);
						abortController.abort(error);
						reject(error);
					}, timeout);
				})]);
				if (result.done) {
					completed = true;
					return;
				}
				yield result.value;
			} finally {
				if (timeoutId !== void 0) {
					clearTimeout(timeoutId);
					timeoutId = void 0;
				}
			}
		}
	} finally {
		if (!completed) iterator.return?.().catch(() => void 0);
	}
}
//#endregion
export { DEFAULT_STREAM_IDLE_TIMEOUT, createLinkedAbortController, createStreamIdleTimeoutError, resolveStreamIdleTimeout, withRequestIdleTimeout, withStreamIdleTimeout };

//# sourceMappingURL=stream_timeout.js.map